services:
  - type: web
    name: plastik-ml-api
    env: python
    plan: free
    rootDir: ml_api
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13